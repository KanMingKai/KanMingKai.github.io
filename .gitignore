# 作業系統
Thumbs.db
Desktop.ini
.DS_Store

# 編輯器
.vscode/
.idea/

# Python
__pycache__/
*.pyc

# 暫存
_selftest.txt
*.tmp

# ---------------------------------------------------------------
# 不可公開 —— GitHub Pages 會把 repo 內容變成公開網址,
# 以下檔案即使沒有任何頁面連到它們,只要 commit 進去就能被直接存取。
# ---------------------------------------------------------------

# 公司內部文件
*.pptx

# 專案檢討筆記:內含分工界線、自我評估與寫法建議等坦白內容,
# 對自己有用,但被招募方看到會造成傷害。留在本機,不要進 repo。
home/*.md

# 面試講稿(附人資長／技術長講評,含自我扣分項)
trj_portfolio.txt

# ---------------------------------------------------------------
# 已被正式版取代的舊草稿,全部封存在 _archive/
# 檔案留在本機,只是不上線 —— GitHub Pages 會把 repo 內每個檔案
# 變成公開網址,這些舊版的數字與正式版不一致,被翻到會自相矛盾。
# 要讓某一份重新上線:把它搬出 _archive/ 並接上導覽即可。
# ---------------------------------------------------------------
/_archive/
